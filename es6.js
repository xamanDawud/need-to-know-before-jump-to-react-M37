let student = {
  name: " Ahsan ",
  id: 354334,
};
let arr = [23, 44, 233, 53];

// Template String
let info = `Student name is ${student.name}`;

// Arrow Function
let getReturn = () => 33;
let add = (x) => x + x;
let checkEven = (x) => x % 2 != 0;
let mutltiParamenter = (x, y) => x * y;
let multiLine = (num1, num2) => {
  return num1 / num2;
};
